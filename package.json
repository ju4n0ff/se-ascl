{
  "name": "senascl",
  "version": "0.1.0",
  "private": true,
  "description": "Intérprete de Lengua de Señas Chilena (LSCh) → Texto y Voz",
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "turbo run typecheck",
    "build": "turbo run build",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
